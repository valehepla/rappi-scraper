# Opcional — proxies anti-detección
PROXY_URL=http://user:pass@proxy-host:port

# ScraperAPI (alternativa a proxies propios)
SCRAPER_API_KEY=your_key_here

# Headless mode (true en producción, false para debug)
HEADLESS=true

# Delay mínimo entre requests (segundos)
MIN_DELAY=2.0
